{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.tsx|*.jsx|*.ts|*.js) npx --no-install prettier --write \"$f\" && npx --no-install eslint --fix \"$f\" ;; *.md|*.mdx) npx --no-install prettier --write \"$f\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
